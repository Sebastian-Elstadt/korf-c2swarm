[package]
name = "domain"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
sqlx = { version = "0.8.6", features = ["postgres", "chrono"] }
async-trait = "0.1.89"
uuid = { version = "1", features = ["serde", "v4"] }